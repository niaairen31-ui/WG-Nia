# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.eggs/
# Environment
.env

# Database — never lives in the tree (June 19 incident). Real path: ~/.world_engine/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Disposable harness_say_replay.py fixtures (BRIEF-0027-b) — regenerated by
# `record`/`replay`, never a deliverable; deleted alongside the harness at
# TICKET-0027 stage g.
scripts/harness_say_fixtures/

# Disposable harness_mutation_apply.py fixtures (BRIEF-0027-c) — same
# discipline: regenerated by `record`/`replay`, never a deliverable;
# deleted alongside the harness at TICKET-0027 stage g.
scripts/harness_mutation_fixtures/

# Disposable harness_entity_author_replay.py fixtures (BRIEF-0027-e) — same
# discipline: regenerated by `record`/`replay`, never a deliverable;
# deleted alongside the harness at TICKET-0027 stage g.
scripts/harness_entity_author_fixtures/

# Disposable harness_tick_replay.py fixtures (TICKET-0028, BRIEF-0028-a) —
# same discipline: regenerated by `record`/`replay`, never a deliverable;
# deleted alongside the harness at TICKET-0028's close (stage f).
scripts/harness_tick_fixtures/
